// TODO
keyboard shortcut = cmd + 6

add ngCloak to reduce browser flickering
replace Selection element in sidebar, atm it's different on FF and IE.
refactor #/1 links to a 'home' state
cache busting (reason: browser might cache a app.js that is newer or older than current version)